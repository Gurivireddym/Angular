import { UserdetailsDirective } from './userdetails.directive';

describe('UserdetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new UserdetailsDirective();
    expect(directive).toBeTruthy();
  });
});
